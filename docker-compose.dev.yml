# Stack DEV : DB + Redis + MinIO + Mailpit uniquement
# Le front Next.js tourne en local (npm run dev) pour un hot-reload rapide.
# Lancer :  docker compose -f docker-compose.dev.yml up -d
services:
  postgres:
    image: postgis/postgis:16-3.4
    container_name: lgbt-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: parislgbt
      POSTGRES_USER: lgbt
      POSTGRES_PASSWORD: lgbt
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "5432:5432"

  redis:
    image: redis:7-alpine
    container_name: lgbt-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redisdata:/data

  minio:
    image: minio/minio:latest
    container_name: lgbt-minio
    restart: unless-stopped
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: lgbtminio
      MINIO_ROOT_PASSWORD: lgbtminio-secret
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - miniodata:/data

  minio-init:
    image: minio/mc:latest
    depends_on: [minio]
    entrypoint: >
      /bin/sh -c "
      sleep 5 &&
      mc alias set local http://minio:9000 lgbtminio lgbtminio-secret &&
      mc mb -p local/parislgbt || true &&
      mc anonymous set download local/parislgbt || true
      "

  mailpit:
    image: axllent/mailpit:latest
    container_name: lgbt-mailpit
    restart: unless-stopped
    ports:
      - "1025:1025"
      - "8025:8025"

volumes:
  pgdata:
  redisdata:
  miniodata:
