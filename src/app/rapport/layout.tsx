// Layout autonome pour /rapport — n'utilise pas le layout racine,
// rend juste les enfants comme un fragment.
export default function RapportLayout({ children }: { children: React.ReactNode }) {
  return children;
}
